trigger:
  batch: true
  branches:
    include:
      - main

pr:
  - main

resources:
  repositories:
    - repository: cnp-azuredevops-libraries
      type: github
      ref: refs/heads/master
      name: hmcts/cnp-azuredevops-libraries
      endpoint: "hmcts"

parameters:
  - name: overrideAction
    type: string
    default: plan
    values:
      - plan
      - apply
      - destroy
  - name: environment_components
    type: object
    default:
      - deployment: "sandbox_core"
        environment: "sbox"
        component: "core"
        service_connection: "DCD-CFTAPPS-SBOX"
        dependsOn: "Precheck"
      - deployment: "sandbox_database"
        environment: "sbox"
        component: "database"
        service_connection: "DCD-CFTAPPS-SBOX"
        dependsOn: "sandbox_core"
      - deployment: "sandbox_container_app"
        environment: "sbox"
        component: "container-app"
        service_connection: "DCD-CFTAPPS-SBOX"
        dependsOn:
          - "AppBuildPublish"
          - "sandbox_core"
          - "sandbox_database"
      - deployment: "dev_core"
        environment: "dev"
        component: "core"
        service_connection: "DCD-CFTAPPS-DEV"
        dependsOn: "sandbox_core"
      - deployment: "dev_database"
        environment: "dev"
        component: "database"
        service_connection: "DCD-CFTAPPS-DEV"
        dependsOn: 
          - "dev_core"
          - "sandbox_database"
      - deployment: "dev_container_app"
        environment: "dev"
        component: "container-app"
        service_connection: "DCD-CFTAPPS-DEV"
        dependsOn:
          - "dev_core"
          - "dev_database"
          - "sandbox_container_app"
      - deployment: "ithc_core"
        environment: "ithc"
        component: "core"
        service_connection: "DCD-CFTAPPS-ITHC"
        dependsOn: "dev_core"
      - deployment: "ithc_database"
        environment: "ithc"
        component: "database"
        service_connection: "DCD-CFTAPPS-ITHC"
        dependsOn:
          - "ithc_core"
          - "dev_database"
      - deployment: "ithc_container_app"
        environment: "ithc"
        component: "container-app"
        service_connection: "DCD-CFTAPPS-ITHC"
        dependsOn:
          - "ithc_core"
          - "ithc_database"
          - "dev_container_app"
      - deployment: "stg_core"
        environment: "stg"
        component: "core"
        service_connection: "DCD-CFTAPPS-STG"
        dependsOn: "ithc_core"
      - deployment: "stg_database"
        environment: "stg"
        component: "database"
        service_connection: "DCD-CFTAPPS-STG"
        dependsOn: 
          - "stg_core"
          - "ithc_database"
      - deployment: "stg_container_app"
        environment: "stg"
        component: "container-app"
        service_connection: "DCD-CFTAPPS-STG"
        dependsOn:
          - "stg_core"
          - "stg_database"
          - "ithc_container_app"
      - deployment: "prod_core"
        environment: "prod"
        component: "core"
        service_connection: "DCD-CFTAPPS-PROD"
        dependsOn: "stg_core"
      - deployment: "prod_database"
        environment: "prod"
        component: "database"
        service_connection: "DCD-CFTAPPS-PROD"
        dependsOn: 
          - "prod_core"
          - "stg_database"
      - deployment: "prod_container_app"
        environment: "prod"
        component: "container-app"
        service_connection: "DCD-CFTAPPS-PROD"
        dependsOn:
          - "prod_core"
          - "prod_database"
          - "stg_container_app"

variables:
  - name: timeoutInMinutes
    value: 60
  - name: agentImage
    value: ubuntu-latest
  - name: product
    value: csds
  - name: terraformInitSubscription
    value: 04d27a32-7a07-48b3-95b8-3c8691e1a263
  - template: vars/input-variables.yaml@cnp-azuredevops-libraries
  - name: imageRepository
    value: csds/pss-test-harness

stages:
  - stage: AppBuildPublish
    displayName: "App Build & Publish"
    jobs:
      - job: BuildAndPublish
        pool:
          vmImage: ${{ variables.agentImage }}
        steps:
          - task: AzureCLI@2
            displayName: "Build and push Docker image to ACR (az acr build)"
            inputs:
              azureSubscription: DCD-CNP-Prod
              scriptType: bash
              scriptLocation: inlineScript
              workingDirectory: $(System.DefaultWorkingDirectory)/PSSTestHarness
              inlineScript: |
                echo "Starting ACR build for $(imageRepository)"

                # Determine the correct branch name
                if [ "$(Build.Reason)" = "PullRequest" ]; then
                  BRANCH_NAME="$(System.PullRequest.SourceBranch)"
                else
                  BRANCH_NAME="$(Build.SourceBranch)"
                fi

                # Remove refs/heads/ prefix if present
                BRANCH_NAME="${BRANCH_NAME#refs/heads/}"

                # Replace / with - for valid Docker tag names
                BRANCH_NAME="${BRANCH_NAME//\//-}"

                echo "Using branch name: $BRANCH_NAME"

                az acr build --registry hmctsprod \
                  -t $(imageRepository):$(Build.BuildId) \
                  -t $(imageRepository):$BRANCH_NAME \
                  -t $(imageRepository):$BRANCH_NAME-$(Build.BuildId) \
                  .

  - stage: Precheck
    dependsOn: []
    jobs:
      - job:
        pool:
          vmImage: ${{ variables.agentImage }}
        steps:
          - template: steps/terraform-precheck.yaml@cnp-azuredevops-libraries
            parameters:
              keyvaultName: "infra-vault-nonprod"
              keyvaultSecret: "azure-devops-sp-token"
              serviceConnection: "azurerm-sandbox"
              overrideAction: ${{ parameters.overrideAction }}

  - ${{ each deployment in parameters.environment_components }}:
      - stage: ${{ deployment.deployment }}
        dependsOn: ${{ deployment.dependsOn }}
        jobs:
          - job: TerraformPlanApply
            pool: hmcts-cftptl-agent-pool
            timeoutInMinutes: ${{ variables.timeoutInMinutes }}
            steps:
              - template: steps/terraform.yaml@cnp-azuredevops-libraries
                parameters:
                  overrideAction: ${{ parameters.overrideAction }}
                  environment: ${{ deployment.environment}}
                  component: ${{ deployment.component }}
                  serviceConnection: ${{ deployment.service_connection }}
                  product: ${{ variables.product }}
                  terraformInitSubscription: ${{ variables.terraformInitSubscription }}
